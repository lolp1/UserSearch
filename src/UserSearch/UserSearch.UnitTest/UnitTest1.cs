namespace UserSearch.UnitTest
{
    [TestClass]
    public class UnitTest1
    {
        /* Add Test for testable services, models, etc. */
        /* In real project, also intergrations test..*/
        [TestMethod]
        public void XmlSerialization_SerializeServiceTest()
        {
            // ISerializationService service = new XmlSerializationService();
            // var integerList = new List<int> {  1, 2, 3};
            // var exported = service.ExportToString(integerList);
            //Assert.AreEqual(@"literalStringRepresentingXmlListOf123Int", exported);
            //
        }
    }
}